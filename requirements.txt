# Core runtime dependencies
# Install with:
#   pip install -r requirements.txt
#
# NOTE:
# - `kubernetes` is required for `collect` and `report` commands.
# - `analyze` can run offline from an existing CSV.

kubernetes>=12.0.1
jinja2>=3.1.6
requests>=2.34.2
pyyaml>=6.0.3
aiohttp>=3.14.0  # transitive via kubernetes; pin to avoid GHSA-jg22-mg44-37j8 / GHSA-hg6j-4rv6-33pg
